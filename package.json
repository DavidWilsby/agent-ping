{
  "name": "agent-ping",
  "version": "2.0.0",
  "description": "Sound alerts and notifications when Claude needs your attention — Claude Code plugin",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/DavidWilsby/agent-ping.git"
  },
  "bugs": {
    "url": "https://github.com/DavidWilsby/agent-ping/issues"
  },
  "homepage": "https://github.com/DavidWilsby/agent-ping#readme",
  "scripts": {
    "build": "tsc && npx esbuild src/mcp-server.ts --bundle --platform=node --format=esm --minify --tree-shaking=true --outfile=bin/mcp-server.js",
    "watch": "tsc --watch",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "@types/node": "^20.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@inquirer/input": "^4.0.0",
    "@inquirer/select": "^4.0.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.3.6"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": [
      "**/tests/**/*.test.ts"
    ]
  }
}
