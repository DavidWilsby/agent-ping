{
  "name": "agent-ping",
  "version": "2.0.0",
  "description": "Sound alerts and notifications when Claude needs your attention — Claude Code plugin",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/DavidWilsby/agent-ping.git"
  },
  "bugs": {
    "url": "https://github.com/DavidWilsby/agent-ping/issues"
  },
  "homepage": "https://github.com/DavidWilsby/agent-ping#readme",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "@types/node": "^20.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@inquirer/select": "^4.0.0",
    "@inquirer/input": "^4.0.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": [
      "**/tests/**/*.test.ts"
    ]
  }
}
