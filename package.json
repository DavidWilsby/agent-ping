{
  "name": "agent-ping-vscode",
  "displayName": "Agent Ping for Claude",
  "version": "1.5.0",
  "description": "Plays a sound when Claude finishes responding, asks a question, or needs your permission — so you can step away and come back when needed.",
  "publisher": "dawi",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/DavidWilsby/agent-ping.git"
  },
  "bugs": {
    "url": "https://github.com/DavidWilsby/agent-ping/issues"
  },
  "homepage": "https://github.com/DavidWilsby/agent-ping#readme",
  "icon": "icon.png",
  "keywords": [
    "claude",
    "ai",
    "agent",
    "sound",
    "notification",
    "audio",
    "ping",
    "cursor",
    "windsurf",
    "hook"
  ],
  "galleryBanner": {
    "color": "#1e1e2e",
    "theme": "dark"
  },
  "main": "dist/extension.js",
  "bin": {
    "agent-ping-vscode": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "jest",
    "prepublish": "npm run build",
    "vscode:prepublish": "npm run build"
  },
  "engines": {
    "vscode": "^1.74.0"
  },
  "activationEvents": [
    "onStartupFinished"
  ],
  "categories": [
    "Other"
  ],
  "contributes": {
    "configuration": {
      "title": "Agent Ping",
      "properties": {
        "agentPing.enabled": {
          "order": 1,
          "type": "boolean",
          "default": true,
          "description": "Enable or disable Agent Ping entirely."
        },
        "agentPing.alertMode": {
          "order": 2,
          "type": "string",
          "default": "sound",
          "enum": [
            "sound",
            "notification",
            "both"
          ],
          "enumDescriptions": [
            "Play a sound only (default behavior)",
            "Show an OS notification banner only (no sound)",
            "Play a sound and show an OS notification banner"
          ],
          "description": "Controls whether Agent Ping plays a sound, shows an OS notification banner, or both."
        },
        "agentPing.respectDnd": {
          "order": 3,
          "type": "boolean",
          "default": false,
          "description": "Suppress sounds when any macOS Focus mode is active (Do Not Disturb, Work, etc.). Requires macOS accessibility permissions on first use. Notification banners are always managed by macOS — configure allowed apps in System Settings → Focus. Has no effect on other platforms."
        },
        "agentPing.volume": {
          "order": 4,
          "type": "integer",
          "default": 50,
          "minimum": 0,
          "maximum": 100,
          "markdownDescription": "Global volume for all Agent Ping sounds (0 = mute, 100 = full volume).\n\n[Reset to default](command:agentPing.resetVolume)"
        },
        "agentPing.notificationEnabled": {
          "order": 5,
          "type": "boolean",
          "default": true,
          "description": "Enable or disable the Notification event sound."
        },
        "agentPing.notificationSound": {
          "order": 6,
          "type": "string",
          "default": "",
          "markdownDescription": "Path to a WAV, MP3, or AIFF (.aiff or .aif) sound file. Leave empty to use the bundled default.\n\n[Choose file…](command:agentPing.chooseNotificationSound) · [Test sound](command:agentPing.testNotificationSound) · [Reset to default](command:agentPing.resetNotificationSound)"
        },
        "agentPing.idlePromptEnabled": {
          "order": 7,
          "type": "boolean",
          "default": false,
          "description": "Play the notification sound when Claude is waiting for your input (idle prompt)."
        },
        "agentPing.stopEnabled": {
          "order": 8,
          "type": "boolean",
          "default": true,
          "description": "Enable or disable the Stop event sound."
        },
        "agentPing.stopSound": {
          "order": 9,
          "type": "string",
          "default": "",
          "markdownDescription": "Path to a WAV, MP3, or AIFF (.aiff or .aif) sound file. Leave empty to use the bundled default.\n\n[Choose file…](command:agentPing.chooseStopSound) · [Test sound](command:agentPing.testStopSound) · [Reset to default](command:agentPing.resetStopSound)"
        }
      }
    },
    "commands": [
      {
        "command": "agentPing.resetVolume",
        "title": "Agent Ping: Reset Volume to Default"
      },
      {
        "command": "agentPing.chooseStopSound",
        "title": "Agent Ping: Choose Stop Sound"
      },
      {
        "command": "agentPing.testStopSound",
        "title": "Agent Ping: Test Stop Sound"
      },
      {
        "command": "agentPing.resetStopSound",
        "title": "Agent Ping: Reset Stop Sound to Default"
      },
      {
        "command": "agentPing.chooseNotificationSound",
        "title": "Agent Ping: Choose Notification Sound"
      },
      {
        "command": "agentPing.testNotificationSound",
        "title": "Agent Ping: Test Notification Sound"
      },
      {
        "command": "agentPing.resetNotificationSound",
        "title": "Agent Ping: Reset Notification Sound to Default"
      }
    ]
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.74.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.0.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": [
      "**/tests/**/*.test.ts"
    ]
  },
  "dependencies": {
    "@inquirer/select": "^4.0.0",
    "@inquirer/input": "^4.0.0"
  }
}
