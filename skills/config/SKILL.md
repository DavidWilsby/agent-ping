---
name: config
description: Configure agent-ping sound alerts and notification settings. Use when the user wants to change agent-ping settings like volume, alert mode, or custom sounds.
---

Call the `mcp__agent-ping__configure` tool to open the Agent Ping settings form. The form lets the user change all settings interactively.
