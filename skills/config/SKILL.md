---
name: config
description: Configure agent-ping sound alerts and notification settings
disable-model-invocation: true
user-invocable: true
allowed-tools: mcp__agent-ping__configure
---

Call the `mcp__agent-ping__configure` tool to open the Agent Ping settings form. The form lets the user change all settings interactively.
